#ifndef CONFIG_H
#define CONFIG_H

// Change this to your development machine's IP address
// Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
#define SERVER_HOST "192.168.50.185"
#define SERVER_PORT 3333

#endif // CONFIG_H
